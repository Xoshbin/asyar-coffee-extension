{
  "name": "org.asyar.coffee",
  "version": "1.0.0",
  "description": "Prevent your computer from sleeping. Cross-platform sleep-prevention utility.",
  "type": "module",
  "scripts": {
    "dev": "asyar dev",
    "build": "asyar build",
    "publish": "asyar publish",
    "link": "asyar link",
    "test": "vitest",
    "test:run": "vitest run",
    "package": "npm run build && zip -r extension.zip dist manifest.json"
  },
  "dependencies": {
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "asyar-sdk": "workspace:*",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@types/rollup-plugin-css-only": "^3.1.2",
    "rollup-plugin-css-only": "^4.5.2",
    "typescript": "^5.0.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0",
    "@vitest/coverage-v8": "^2.1.0"
  },
  "optionalDependencies": {
    "@rollup/rollup-win32-arm64-msvc": "*"
  }
}
